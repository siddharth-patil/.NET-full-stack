#include<stdio.h>
#include<string.h>

int main()
{
    char name[] = "Siddharth Patil";
    int nameLength;

    nameLength = strlen(name);

    printf("Length of string is: %d",nameLength);

    return 1;
}
