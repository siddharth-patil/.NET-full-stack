import { DisableControl } from './disable-control';

describe('DisableControl', () => {
  it('should create an instance', () => {
    const directive = new DisableControl();
    expect(directive).toBeTruthy();
  });
});
